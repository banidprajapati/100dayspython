# 2 8 5 3 9 4 1 7
# get user input
user_list = []


def userInput():
    # user_input = input(
    # "Enter a list of number to sort with space in between: \n").split(" ")
    # user_list = [int(n) for n in user_input]
    user_list = [2, 8, 5, 3, 9, 4, 1, 7]
    return user_list


def mergeSort(list):
    if len(list) > 1:
        leftList, rightList = [], []
        for i in len(list):
            # leftList = [list[i] if i < len(list) / 2]
            print(leftList)
    pass


# mergeSort(userInput())
